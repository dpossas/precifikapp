import 'package:flutter/material.dart';

class EPDialogTheme {
  static DialogTheme get themeData => const DialogTheme(
        backgroundColor: Colors.white,
        elevation: 0,
        insetPadding: EdgeInsets.all(24),
      );
}
